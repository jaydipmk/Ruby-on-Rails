<h2>Sign Up</h2>
<div class="row">
  <div class="col-md-6 col-md-offset-3" >
    <%= form_for(@emp, url: login_master_index_path) do |f| %>
      <%#= render 'shared/form' %>
      <%= f.label :username %>
			<%= f.text_field :username, class: 'form-control' %>
			<br>
			<%= f.label :password %>
			<%= f.password_field :password, class: 'form-control' %>
			<br>
			<%= f.label :Role %>
			<%= f.select :role, ['Admin', 'Employee']%>
			<br>
			<%= f.submit "Sign up and Continue" , class: "btn btn-primary" %> or
			<%= link_to "Sign in with google", "/auth/google_oauth2" %>
			<br>
    <% end %>
  </div>
</div>
